# praktika_mod_1

CLI для сравнения двух файлов транзакций в любых поддерживаемых форматах.

```
cargo run -p ypbank_converter -- --file1 <file_name> --format1 <bin|csv|txt> --file2 <file_name> --format2 <bin|csv|txt>
```
