# praktika_mod_1

CLI для сравнения двух файлов транзакций в любых поддерживаемых форматах.

```
cargo run -p ypbank_converter -- --input <file_name> --input-format <bin|csv|txt> --output-format <bin|csv|txt>
```
